import React from 'react';
import Headerlogo from './headerlogo/headerlogo';
const Header = (props) => {
    return (
        <header>
            <Headerlogo />
        </header>
    );
}
export default Header;